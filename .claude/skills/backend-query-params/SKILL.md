# backend-query-params
## Description
Standardized approach for handling filtering, sorting, and pagination via URL query parameters.

## Core Principles
1. **Consistency**: Use standard keys (e.g., `page`, `limit`, `sort_by`).
2. **Type Safety**: Cast parameters to correct types (int, date, etc.).
3. **Defaults**: Provide sensible default values.
